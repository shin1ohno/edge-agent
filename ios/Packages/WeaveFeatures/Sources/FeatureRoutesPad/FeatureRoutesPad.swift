import SwiftUI

/// iPad-only Routes editor (Sidebar + canvas wire + Inspector). Phase 6
/// scope — see `#ipad-routes` in the hi-fi mockup. Wire drawing will be
/// `Canvas` + `Path` per the handoff §6 Phase 6 note.
public struct RoutesPadRootView: View {
    public init() {}

    public var body: some View {
        Text("Routes editor — iPad — placeholder")
            .foregroundStyle(.secondary)
    }
}
