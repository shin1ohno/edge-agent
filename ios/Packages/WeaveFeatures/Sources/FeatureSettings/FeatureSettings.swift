import SwiftUI

/// Settings tab. Phase 2 ships the Settings.app-style inset list — see
/// `#ios-settings-light` / `#ios-settings-dark`.
public struct SettingsRootView: View {
    public init() {}

    public var body: some View {
        Text("Settings — placeholder")
            .foregroundStyle(.secondary)
    }
}
