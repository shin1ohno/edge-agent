import SwiftUI

/// Phase 1 placeholder. Real 4-step onboarding (Welcome / Bluetooth /
/// Pair scan / Server *optional*) lands in Phase 2 — see
/// `#ios-onboarding` in `weave hi-fi mockup.html`.
public struct OnboardingFlowView: View {
    public init() {}

    public var body: some View {
        Text("Onboarding — placeholder")
            .foregroundStyle(.secondary)
    }
}
