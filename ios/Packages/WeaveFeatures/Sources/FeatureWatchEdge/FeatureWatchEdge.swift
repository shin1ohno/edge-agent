import SwiftUI

/// watchOS edge-agent UI surface. Phase 7 implements the 4 states
/// (Crown rotate / tap=press / swipe / paired idle) — see
/// `#native-watch` in the hi-fi mockup.
public struct WatchEdgeRootView: View {
    public init() {}

    public var body: some View {
        Text("Watch edge — placeholder")
            .foregroundStyle(.secondary)
    }
}
